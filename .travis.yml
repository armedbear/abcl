language: java

jdk:
  - openjdk8

os:
  - linux
# -osx

dist: xenial

install:
  - ant abcl

script:
  ./abcl --eval '(require :abcl)' --eval '(require :abcl-contrib)' --eval '(asdf:load-system :quicklisp-abcl :force t)'

  


  
